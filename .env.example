# AskAHuman API endpoint
ASKAHUMAN_API_URL=http://localhost:8080

# L402 credentials (obtained from first 402 challenge)
# These are cached automatically after first payment
# ASKAHUMAN_MACAROON=
# ASKAHUMAN_PREIMAGE=

# LND connection (optional — only needed if MCP server handles payments directly)
# LND_REST_HOST=localhost
# LND_REST_PORT=8080
# LND_MACAROON_PATH=
# LND_TLS_CERT_PATH=
